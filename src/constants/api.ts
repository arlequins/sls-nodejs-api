export const enum API_ERROR_MESSAGES {
  BAD_PARAMS = 'bad params',
  UNKNOWN_ERROR = 'unknown error',
  CANNOT_GET_USER = 'cannot get user',
  CANNOT_MAKE_USER = 'cannot make user',
}

export const enum API_PAYLOAD_RESULT {
  GREEN = 'GREEN',
  ERROR = 'ERROR',
  SERVICE_STOPPED = 'SERVICE_STOPPED',
}

export const enum API_METHODS {
  GET = 'GET',
  POST = 'POST',
}
